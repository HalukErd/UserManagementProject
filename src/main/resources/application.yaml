spring:

  jpa:
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


    hibernate:
      ddl-auto: create-drop

  datasource:
    url: jdbc:postgresql://localhost:5433/avansas
    username: postgres
    password: superpassword
    driver-class-name: org.postgresql.Driver

server:
  port: 8080
